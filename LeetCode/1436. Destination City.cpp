class Solution {
public:
    string destCity(vector<vector<string>>& paths) {
        string ans;
        map<string, string> mp;
        for (auto i : paths) {
            mp[i[0]] = i[1];

        }
        while (ans) {

        }
    }
};